# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54, not the latest SDK: as of August 2026, Expo Go's Apple App Store build is stuck at SDK 54 (SDK 55/56/57 builds have been stuck in App Store review for months — see `mobile/README.md`'s "Testing on a physical device" section). Do not bump past SDK 54 without checking whether that's still true.
